apiVersion: v1
kind: Service
metadata:
  name: fraud-api
  namespace: ml-platform
  labels:
    app: fraud-api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: fraud-api
